<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.flannery.multilanguage">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidHelper">
        <activity android:name=".WebpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ExpandActivity">
        </activity>
        <activity android:name=".confict4.Conflict4Activity" />
        <activity android:name=".confict5.Conflict5Activity"></activity>
        <activity android:name=".NestedScrollActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".ConflictActivity" />
        <activity android:name=".ConflictActivity2" />
        <activity android:name=".ConflictActivity3" />
        <activity android:name=".ThirdActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>